Run the script using these commands
```
nvcc cuda_files/01_idxing.cu -o bin/hello && ./bin/hello
```

This is my laptop's output when I ran ./deviceQuery (It's kinda old but it gets shit done hehe!)
<img src=""/>